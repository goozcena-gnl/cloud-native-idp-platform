name: Plumber CI/CD Security

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

concurrency:
  group: plumber-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  plumber:
    name: Plumber gate
    runs-on: ubuntu-24.04
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Analyze GitHub Actions security
        uses: getplumber/plumber@de6c889e24c9147baa064db656b71c8e98595d15  # v0.5.8
        with:
          version: v0.5.2
          verify-attestation: true
          config-file: .plumber.yaml
          min-points: 100
          fail-warnings: true
          soft-fail: false
          score-push: false
          upload-sarif: true
          upload-artifacts: true
          artifact-name: plumber-security-reports
          output: plumber-report.json
          sarif: plumber.sarif
          pbom: plumber-pbom.json
          pbom-cyclonedx: plumber-cyclonedx-sbom.json
